# from marshmallow import Schema, fields

# class OutputSchema(Schema):
#     uses = fields.List(dump_only = True)

# # class LeafOutputSchema(Schema):
# #     id = fields.Integer(dump_only = True)
# #     name = fields.String(required=True)